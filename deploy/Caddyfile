{
    email your-email@example.com
}

gymflow.codebshift.dev {
    encode gzip

    @api {
        path /api/* /stripe/webhook
    }

    # Backend (Spring Boot) – all /api/** and /stripe/webhook
    reverse_proxy @api api:8080

    # Frontend – everything else
    reverse_proxy web:80
}
